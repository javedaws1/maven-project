pipeline {
    agent any


    stages {
        stage('SCM Checkout'){
          git 'https://github.com/javedaws1/maven-project'
        }
  }
    {
        stage ('Compile Stage') {

            steps {
                withMaven(maven : 'JenkinsLocalMaven') {
                    sh 'mvn clean compile'
                }
            }
}
}
    {
        stage ('test Stage') {

            steps {
                withMaven(maven : 'JenkinsLocalMaven') {
                    sh 'mvn test'
                }
            }
}
}
    {
        stage ('package Stage') {

            steps {
                withMaven(maven : 'JenkinsLocalMaven') {
                    sh 'mvn package'
                }
            }
}
}
    {
        stage ('deploy Stage') {

            steps {
               sshagent (credentials: ['e58beaa6-b393-4e31-9d16-c509821c53ca']) {
               sh 'scp -o StrictHostKeyChecking=no */target/webapps/*.war ec2-user@18.221.138.218:/usr/share/tomcat'
               }
            }
}
}
}
